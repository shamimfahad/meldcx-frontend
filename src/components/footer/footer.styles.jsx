import styled from 'styled-components';

import { AlignAndJustifyCenter } from '../utils/utllComponents.styles';

export const StyledFooter = styled(AlignAndJustifyCenter)`
  padding: 1rem 0;
  background: rgba(0, 0, 0, 0.1);
  position: fixed;
  bottom: 0;
  width: 100%;
`;
